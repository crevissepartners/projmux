# .deadcode-must-keep.txt
#
# Proactive maintenance ledger for symbols that must survive for migration,
# compatibility, or proof contracts, whether or not they are current deadcode
# findings.
# Each data row is exactly `symbol<TAB>maintenance reason`; reasons are
# mandatory. This file suppresses only its exact, reason-bearing symbols when
# they appear in deadcode output; unrelated new findings still fail the gate.

AggregateTeardownEvents	Pure multi-event teardown authority is called by the product path and retained for lifecycle decision proofs.
All	Closed provider-registry projection retained as the test seam that pins the live aiprovider registry create-shortcut and picker-order contract.
AuditGoStringLiteralsInDir	Directory walker of the hardcoded-string governance audit; its subject is the live repository source, so the guard is test-entered by design rather than a retired product path.
AuditGoStrings	In-memory entry point of the hardcoded-string governance audit retained with AuditGoStringLiteralsInDir as the guard that keeps catalog-bypassing literals out of shipped code.
Binding.ControlAuthority	The snapshot-barrier control authority accessor is retained as the in-process mutation gate; the local IPC path fences inside Submit instead.
Broker.WriteLedger	The mutation write ledger is retained as the observable no-replay evidence surface.
BuildSwitchPickerItems	Backend-neutral switch picker row builder retained as the test seam over the live switchPickerItem projection.
CanonicalRoutes	The canonical route table accessor is retained for cross-package coverage and generated-reference consumers.
Catalog.Keys	Whole-catalog key enumeration retained as the test seam that pins the shipped default catalog key coverage.
Catalog.LocaleKeys	Per-locale key enumeration retained as the test seam that pins the shipped catalog per-locale coverage.
Catalog.MissingFallbackKeys	Fallback-locale projection of MissingLocaleKeys retained as the test seam that pins required-key coverage of the shipped catalog.
ClassifySchemaVersion	Ambient-environment entry point of ClassifySchemaVersionWith retained as the test seam that pins fail-closed registry schema classification.
CleanupLegacyArtifacts	Persisted usage-state cleanup must remain available across legacy state migrations.
CurrentProtocol	The build's local IPC version window accessor is retained as the negotiation input an explicit caller supplies.
DecideAuthority	Phase 0 pure five-axis Codex authority decision is retained for the generation-aware projection phase.
DecideTeardownEvent	The pure teardown decision API is called by the product path and retained for lifecycle authority proofs.
DecodeCapabilityLedger	Test-only installed qualification API retained for strict checked-in capability-ledger validation.
DefaultOpener	The original default stdio-proxy broker opener remains a compatibility seam while bounded lifecycle routing uses direct witnessed Unix attachment.
DirectEndpoint.Close	Test-only installed qualification API retained for bounded direct endpoint cleanup proof.
DirectEndpoint.Health	Test-only installed qualification API retained for canonical direct readiness evidence.
DirectEndpoint.forceCleanup	Test-only installed qualification helper retained for injected direct startup-failure cleanup proof.
DirectEndpoint.markClosed	Test-only installed qualification helper retained for single-consumer bounded direct exit finalization proof.
Discover	The background-context discovery wrapper is retained as a test and compatibility seam.
Dispositions	Closed CLI route disposition catalog retained as the test seam that pins exactly one disposition per canonical route.
EqualTeardownPlans	Serialization-free teardown plan comparison retained as the comparison primitive of the live teardown aggregation property tests.
EvaluateTurnFreeThreadLiveAttach	Test-only installed qualification API retained for method-independent semantic capability reduction.
Fixture.ApplyEnv	Test-only installed qualification API retained for isolated environment containment proof.
Fixture.Cleanup	Test-only installed qualification API retained for exact owned-artifact cleanup proof.
Fixture.Ledger	Test-only installed qualification API retained for semantic ambient non-mutation proof.
Fixture.ProvisionManagedPayload	Test-only installed qualification API retained for managed provision classification proof.
Fixture.RunManagedLifecycle	Test-only installed qualification API retained for managed start/reuse/retire proof.
Fixture.StartDirect	Test-only installed qualification API retained for disposable direct endpoint readiness proof.
Fixture.StartRemoteNewPane	Test-only Phase 1 qualification API retained for exact RoleTUI remote-new first-input evidence.
Fixture.StartTurnFreeAttachPane	Test-only installed qualification API retained for exact isolated live-Pane capability evidence.
Fixture.Versions	Test-only installed qualification API retained for complete version-tuple evidence.
Fixture.cleanupSupportArtifacts	Test-only installed qualification helper retained for exact ledger and shim cleanup proof.
Fixture.discoverCLI	Test-only installed qualification helper retained for installed CLI version evidence.
Fixture.observeDaemonVersion	Test-only installed qualification helper retained for managed terminal version evidence.
Fixture.observeHealth	Test-only installed qualification helper retained for canonical readiness version evidence.
Fixture.readDaemonVersion	Test-only installed qualification helper retained for managed socket and version observation proof.
Fixture.readManagedPID	Test-only installed qualification helper retained for contained public PID artifact proof.
Fixture.readManagedStartResult	Test-only installed qualification helper retained for exact managed start PID proof.
Fixture.retireAfterFailure	Test-only installed qualification helper retained for bounded exact managed failure cleanup proof.
FooterBlockLines	Default-theme entry point of FooterBlockLinesWithTheme retained as the picker footer geometry seam.
FormatList	Locale-resolving list formatter retained as the test seam that pins payload-item preservation over the live formatter locale resolution.
FoundationKeys	Foundation key set retained as the test seam that pins the shipped default catalog coverage of it.
HeaderLine	Default-theme entry point of HeaderLineWithTheme retained as the picker chrome golden seam.
Host.RuntimeID	The runtime identity accessor is retained as the authority axis clients present alongside both epochs.
IsProjectConfigTrusted	Ambient trust-store read retained as the test seam over the live loadTrustedProjects path.
IsSharedOutputMode	Closed shared -o catalog membership predicate retained as the seam that keeps route-local projections out of the shared enum.
IsolatedPane.Alive	Test-only installed qualification API retained for exact pane-dead and socket-identity observation.
IsolatedPane.Close	Test-only installed qualification API retained for contained exact tmux server cleanup.
IsolatedPane.ID	Test-only installed qualification API retained for content-free exact Pane evidence.
IsolatedPane.run	Test-only installed qualification helper retained for inherited tmux identity stripping on every command.
Kinds	Closed Registry kind set retained as the test seam that pins kind-set closure and cardinality matrix coverage.
Ledger.AmbientMutations	Test-only installed qualification API retained for typed ambient mutation counting proof.
Ledger.AssertNoAmbientMutation	Test-only installed qualification API retained for zero ambient mutation proof.
Ledger.AssertNoLifecycleMutation	Test-only installed qualification API retained for broker-smoke lifecycle non-mutation proof.
Ledger.Commands	Test-only installed qualification API retained for semantic command evidence.
Ledger.DistinctOperations	Test-only installed qualification API retained for content-free command reporting.
Ledger.HasOperation	Test-only installed qualification API retained for canonical proxy observation proof.
Ledger.record	Test-only installed qualification helper retained for owned lifecycle command evidence.
ListLinesWithScrollbar	Default-rows entry point of ListLinesWithScrollbarRows retained as the scrollbar thumb geometry seam.
Localizer.Styled	Styled-fragment lookup retained as the test seam that pins the kind-mismatch error of the live catalog lookup.
LookupCanonicalRoute	Canonical spelling lookup is retained for route coverage and generated-reference consumers.
Matrix	Whole cardinality table copy retained as the test seam that pins the declared verb-kind cells and their canonical route adoption.
MigrateGlobalLegacyScripts	Global legacy hook migration must remain available while old installations can still be upgraded.
MigrateProjectLegacyScripts	Project legacy hook migration must remain available while old repositories can still be upgraded.
MigrateRegistry	Ambient-environment entry point of MigrateRegistryWithEnvironment retained as the registry schema migration seam.
NewClean	Test-only installed qualification API retained for canonical clean-root topology proof.
NewExisting	Test-only installed qualification API retained for unchanged model-dependent broker smoke containment.
NewResult	Test-only installed qualification API retained for typed terminal result construction.
NewState	Recent-window state constructor retained as the test seam over the live normalizeEntries MRU contract.
NewStore	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
NewWithConfig	Credential and endpoint injection constructor retained as the only seam that exercises the live Claude usage adapter without real credentials.
NewWithRoot	Rollout-root injection constructor retained as the only seam that pins the live rollout parser without probing the installed Codex.
NextPane	Direction-bound entry point of cyclePane retained as the test seam for the preview cursor semantics Store.CyclePaneSelection ships.
NextWindow	Direction-bound entry point of cycleWindow retained as the test seam for the preview cursor semantics Store.CycleWindowSelection ships.
ObserveFrontReads	Test hook of the settings layer seam; the settings layer guard installs it to count front-layer reads per public route, and production never installs an observer.
OpenPrivateUnix	Test-only private Unix app-server dial is retained for fixed dual-version installed qualification only.
OperatorWebOrigin	The operator-origin constructor is the one sanctioned way to build operator input; no product path calls it until the web sender lands, and TestNoProductPathBuildsAnOperatorOrigin pins that its callers are only the listed producers.
Parse	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
PickerChromeStringAuditOptions	Picker-chrome profile of the governance string audit; it scopes the live guard that keeps picker chrome inside the catalog.
PlanProjectCascadeDelete	Project cascade-delete planner retained as the decision proof for external-asset preservation and new-UID reopen.
PreviousPane	Direction-bound entry point of cyclePane retained as the test seam for the preview cursor semantics Store.CyclePaneSelection ships.
PreviousWindow	Direction-bound entry point of cycleWindow retained as the test seam for the preview cursor semantics Store.CycleWindowSelection ships.
PromptLineWithCursor	Default-theme cursor prompt entry point retained as the picker prompt seam over QueryWithCursorAndTheme.
QualificationResult.Validate	Phase 0 strict generation qualification consistency check is retained for installed receipts.
ReadLifecycleSnapshotOn	The shared requester lifecycle decoder remains a compatibility seam and the independent differential oracle for the owned bounded reader.
ReadRuntimeHealth	The typed no-write runtime-health seam is retained for diagnostics tests and the planned doctor consumer.
RefusalOf	Phase 0 bundle refusal classifier is retained for pre-commit drift and protocol gates.
Registry.rebuildMissingReservations	v0-to-v1 name reservation rebuild retained as the migration proof for the live scopeFor reservation scoping.
Render	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
RenderBar	Uncolored HUD bar renderer retained as the test seam that pins the BarFillCount fill math shared with RenderColoredBar.
RenderSwitchPreview	Default badge-style entry point of RenderSwitchPreviewWithAIBadgeStyle retained as the switch preview golden seam.
Renderer.ContentLayout	Untitled entry point of ContentLayoutWithTitle retained as the frame inner-width seam.
Renderer.RenderFrame	Untitled entry point of RenderFrameWithTitle retained as the frame geometry and CRLF golden seam.
Result.JSON	Test-only installed qualification API retained for terminal JSON evidence.
Result.Validate	Test-only installed qualification API retained for closed class and nonblank tuple validation.
ResumeAdapter.ResumeCommand	Shared Agent CLI resume command renderer retained with the per-provider ResumeCommand wrappers after snapshot replay, its only product caller, was removed.
ResumeCommand	Per-provider Agent CLI resume command wrappers retained as the tested adapter surface after snapshot replay, their only product caller, was removed.
ResumeDefaultThread	The default-endpoint resume wrapper is retained for installed protocol qualification; generation-pinned product routing uses explicit routes.
RouteLocalFieldProjections	Closed route-local field projection catalog retained as the test seam that keeps -o cwd out of the shared enum.
Routes	The public route table accessor is retained for cross-package coverage and generated-reference consumers.
Run	Recorder-free CLI bootstrap retained as the test seam over the live App.Run dispatch that RunWithLifecycleDiagnostics ships.
Runner.ListWindows	Structured window inventory read retained as the test seam that pins the live format join and pane target argument builders.
RuntimeKoreanStringAuditOptions	Runtime Korean profile of the governance string audit; it scopes the live guard over shipped Go sources.
SelectedLine	Default-theme entry point of SelectedLineWithTheme retained as the selected-row gutter seam.
SettingItems	Read-only projection of the settings layer declaration retained as the test seam the settings layer guard seeds every front file from.
SharedOutputModes	Closed shared -o catalog retained as the test seam that pins catalog closure and contract ordering.
SmokeRoot	Test-only installed qualification API retained for canonical isolated root validation.
StartDefaultThread	The default-endpoint start wrapper is retained for installed turn-free protocol qualification; generation-pinned product routing uses explicit routes.
Store.Dir	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
Store.List	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
Store.Load	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
Store.LoadArtifact	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
Store.Migrate	Registry schema migration must remain available for persisted pre-current Registry files.
Store.Path	The Registry path accessor is retained for migration and storage-boundary tests.
Store.Save	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
Store.SetClock	The injected Registry clock is retained as a deterministic migration and transaction test seam.
StringAuditFinding.IsCandidate	Candidate predicate of the governance string audit retained with the guard that classifies shipped literals.
SurfaceSites	Closed unshown-result site inventory; the C-1 committed-result sweep fails until every site is recorded by a registered call site.
TeardownEventKinds	Closed teardown event vocabulary retained as the test seam that pins the teardown decision table totality.
TeardownObservations	The closed teardown-observation catalog is called by the product path and retained for authority proofs.
Text.Locale	Resolved-locale accessor retained as the test seam that pins the live catalog fallback resolution.
UIDKind	UID prefix reader retained as the test seam that pins the live uidPrefixes mint-and-read contract.
ValidateName	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
VersionTuple.normalized	Test-only installed qualification helper retained for explicit unknown version evidence.
WithCodexCatalogThreadReader	Thread-reader injection option retained as the only seam that pins Session State thread resolution without an installed Codex.
WithFileReader	File-reader injection option retained as the only seam that pins client snapshot reads without touching the real filesystem.
WithSocketName	The injected tmux socket option is retained as an exact routing compatibility and test seam.
aiSummaryForKind	Fallback-locale entry point of aiSummaryForKindLocale retained as the AI notification label seam.
applyValue	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
boundedBuffer.Bytes	Test-only installed qualification helper retained for bounded public status decoding.
boundedBuffer.Write	Test-only installed qualification helper retained for bounded installed command capture.
buildFocusURI	Round-trip counterpart of the retained focus URI parser; the test pins that the shipped parser accepts the documented format.
buildPopupToggle	Default-style entry point of buildPopupToggleWithStyle retained as the popup launcher environment seam.
classifyCodexCommand	Test-only installed qualification helper retained for semantic mutation classification.
codexHooksBlock	Default-events entry point of codexHooksBlockForEvents retained as the managed Codex hook block seam.
committedResultDisplaySites	Closed committed-result call-site ledger; the C-1 source sweep fails until a new post-commit result line is registered and classified here.
committedResultSweepFiles	Source files the C-1 committed-result sweep reads; it is the declared scope of that sweep, not a product path.
committedResultSweepTokens	Closed set of ways the interactive adapter layer puts a line on a client; it is what makes the C-1 sweep decidable.
equalArgv	Test-only installed qualification helper retained for closed command-shape classification.
formatRelativeAge	Fallback-locale entry point of formatRelativeAgeLocale retained as the status notify age bucket seam.
formatStatusNotify	Fallback-locale entry point of formatStatusNotifyWithLiveLocale retained as the status notify width tier seam.
genericTeardownRefusal	The generic refusal constructor is retained inside the pure teardown authority proof surface.
isolatedEnvironment	Test-only installed qualification helper retained for CODEX_HOME and tmux identity containment.
legacyDesktopAppID	The retired desktop application identifier remains required to recognize and clean legacy integration state.
legacyWindowAttentionRows	Legacy attention rows remain required while older persisted window attention state is readable.
nativeAppendPartialNextItemLines	Default-theme entry point of nativeAppendPartialNextItemLinesWithTheme retained as the partial multiline item seam.
nativeInteractiveItemLines	Default-theme entry point over InteractiveRowLines retained as the multiline selection seam.
nativeInteractiveListLines	Default-theme entry point over InteractiveListLines retained as the interactive list line seam.
nativePromptLine	Default-theme entry point over projmuxpicker.PromptLine retained as the inline match count seam.
nativePromptLineWithCursor	Default-theme cursor entry point over PromptLineWithCursorLabel retained as the query cursor rendering seam.
nativeRenderableListLine	Default-theme entry point over RenderableListLine retained as the selected-line padding seam.
nativeSelectedContent	Default-theme entry point over SelectedContent retained as the selection style reset seam.
nativeTruncateANSI	Default entry point over projmuxpicker.TruncateANSI retained as the ANSI cut-before-reset seam.
nativeVisibleLen	Default entry point over projmuxpicker.VisibleLen retained as the visible width seam.
newClientWithEnv	Environment injection constructor retained as the only seam that pins tmux client attach and switch routing without a live tmux server.
newFixture	Test-only installed qualification helper retained as the common clean/existing fixture constructor.
newLedger	Test-only installed qualification helper retained for canonical semantic command evidence.
notifyCommand.buildNotifyLiveReport	Fallback-locale entry point of buildNotifyLiveReportLocale retained as the notify row classification seam.
notifySidebarEntriesWithLive	Fallback-locale collapsed-entry projection of the live notify sidebar read model retained as the classification seam.
notifySidebarStateBadge	Live-display entry point of notifySidebarStateBadgeForDisplay retained as the sidebar state palette seam.
observationRefusal	Observation-specific refusal mapping is retained inside the pure teardown authority proof surface.
openReadyThreadClient	The default-endpoint client helper remains behind installed protocol qualification wrappers, not generation-pinned product routing.
parseIntValue	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
parseStringValue	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
pathWithin	Test-only installed qualification helper retained for tmux socket containment proof.
pickerOptionsFromCompatPicker	Compat picker option projection retained as the test seam over the live pickercompat.PickerOptions mapping.
presetEntry	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
projectPreservedAssets	The preserved-assets outcome helper is retained for project teardown authority proofs.
rejectArtifactSymlinks	Project layout preset reader/writer for <project>/.projmux/layouts retained as the supported preset format after the named-snapshot save and the Session picker snapshot view that called it were removed.
remoteNewTMUXArgs	Test-only installed qualification argv allowlist is retained to prove remote-new starts without a prompt, resume operand, or send-keys path.
removeExactResidualSocket	Test-only installed qualification helper retained for ready-time socket identity cleanup proof.
renderNativeInteractive	Cursor-at-end entry point of renderNativeInteractiveWithCursor retained as the native picker frame golden seam.
renderNotifyBadge	Severity badge entry point retained as the test seam over the live renderNotifyBlockBadge palette.
renderSettingsNavTree	Settings navigation tree renderer retained as the golden seam over the live settingsNavChildren topology.
rootDefaults	Root teardown defaults are retained inside the pure teardown decision authority.
runShellOutputLedger	Closed run-shell producer ledger; the C-1 no-overlay contract is enforced by sweeping shipped generated config against it.
runShellSourceSites	Closed run-shell source site ledger; the package source sweep fails until a new producer is registered here.
runtimeMutationPlan.withoutEffects	Effect-free plan projection retained as the property-test seam that pins guarded, ordered, idempotent runtime mutation.
settingsCommand.rootEntries	Fallback-locale entry point of the settings root entry builder retained as the axis metadata seam.
shellJoin	Shell argv joiner behind the retained ResumeAdapter.ResumeCommand renderer.
shellQuote	Shell quoting helper behind the retained ResumeAdapter.ResumeCommand renderer.
sortNameReservations	Stable reservation ordering used by the retained rebuildMissingReservations migration proof.
sortedProbeLabels	Ordering helper retained as the test seam that pins the live default probe key catalog coverage of the terminal key spec.
tmuxAppConfig	Default-keymap entry point of tmuxAppConfigWithKeymap retained as the generated managed config golden seam.
tmuxCapabilityEnvironment	Test-only installed qualification helper retained for per-command tmux and CODEX_HOME isolation.
tmuxPaneBorderFormat	Default badge-style entry point of tmuxPaneBorderFormatWithAIBadgeStyle retained as the pane border format seam.
tmuxRetiredKeyUnbindLines	Retired key cleanup must remain available so old generated tmux bindings can be removed.
tmuxStandaloneConfig	Default-keymap entry point of tmuxStandaloneConfigWithKeymap retained as the standalone config golden seam.
tmuxStyledVisiblePaneLabelFormat	Default-theme entry point of tmuxStyledVisiblePaneLabelFormatFor retained as the visible pane label format seam.
unsupportedRootsError	The default-endpoint roots refusal helper remains part of the retained installed protocol qualification contract.
validOwnerChain	Owner-chain validation is retained inside the pure teardown decision authority.
validTeardownEventKind	The closed event-kind validator is retained inside the pure teardown decision authority.
validTeardownObservation	The closed observation validator is retained inside the pure teardown decision authority.
validateCapabilityTMUXSocketRoot	Test-only installed qualification helper retained to reject overlong private tmux routes before lifecycle mutation.
validateInheritedEnvironment	Test-only installed qualification helper retained for inherited tmux refusal proof.
validateManagedIdentity	Test-only installed qualification helper retained for foreign PID/socket cleanup refusal proof.
versionOrUnknown	Test-only installed qualification helper retained for nonblank version-tuple evidence.
waitForProcessExit	Test-only installed qualification helper retained for independently bounded direct kill-wait proof.
waitForRetirement	Test-only installed qualification helper retained for bounded PID/socket retirement proof.
writeLedgerShim	Test-only installed qualification helper retained for complete semantic command observation.
