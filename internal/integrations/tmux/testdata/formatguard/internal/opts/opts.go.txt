// Package opts holds fixture tmux names.
package opts

// PaneVar is a tmux format variable.
const PaneVar = "pane_id"

// UserOption is a projmux user option.
const UserOption = "@projmux_fixture"

// MouseFormat expands an option name.
const MouseFormat = "#{mouse}"
