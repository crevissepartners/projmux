projmux doctor
read-only diagnostics; displayed remediation is never executed

Dependencies
  Summary: 4 ok, 0 missing, 0 stale, 0 skipped, 0 hint.

Runtime
  No runtime checks in schema version 1; runtime health is planned for Phase 2.

AI notify integrations
  Summary: 0 installed, 0 missing, 0 stale, 0 conflict, 0 skipped.

Session State resume metadata
  Summary: 0 available, 0 stale, 0 unavailable.

Session State retention
  Snapshots are never automatically pruned; inspect stale candidates with `projmux prune session-state` and delete only by explicit name.

Logs
  No log checks in schema version 1; log health is planned for Phase 2.
